export enum ErrorLevel {
  FATAL = 'fatal',
  ERROR = 'error',
  WARNING = 'warning',
  INFO = 'info'
}

export function isValidErrorLevel(level: unknown): level is ErrorLevel {
  return Object.values(ErrorLevel).includes(level as ErrorLevel);
}

export default ErrorLevel;
